Install the Gas City provider into BB

gc bb install [--yes]

Requires Node.js 22+, npm, and the BB CLI. Copies the adapter into the operator data directory, installs pinned dependencies, and invokes BB plugin installation. Pass --yes to BB only when you want to skip its installation prompt.
