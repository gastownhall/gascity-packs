List exact agent IDs and their scope

gc bb agents [--project <BB-project-ID> | --cwd <workspace>] [--json]

With no project or directory, lists globals from all running cities. A project shows its city globals and matching rig agents. The output uses the same discovery implementation as the BB provider.
