Check provider connections and mappings

gc bb status [--json]

Checks supervisor compatibility and reports connection failures. JSON also includes host-local configuration and journal paths, never authentication tokens.
