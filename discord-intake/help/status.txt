Show the current Discord intake configuration, published URLs, and recent
requests.

Examples:
  gc discord-intake status
  gc discord-intake status --json

The snapshot includes the public interactions URL, the tenant admin URL,
redacted app configuration, channel mappings, and recent `/gc fix` requests.
