{
  "model": "jev-1.13.0",
  "state": {
    "request": "Issue title: bug(doctor): custom-types probe inherits ambient BEADS_DOLT_SERVER_*/GC_DOLT_* \u2192 false \"could not read types.custom\" for external-target stores\n\n## Summary\n\n`gc doctor`'s `custom-types:<scope>` check shells out to `bd config get --json\ntypes.custom` but does **not** set `cmd.Env`, so the probe subprocess inherits\nthe caller's ambient environment. When `gc doctor` runs in a shell that exports\nDolt-server coordinates for one store (e.g. `BEADS_DOLT_SERVER_HOST` /\n`BEADS_DOLT_SERVER_PORT` for an HQ/managed store), those variables steer the\nprobe at the **wrong** server for every *other* store it checks. For a rig whose\nstore lives on a different Dolt server, `bd` then connects to the inherited\nhost/port, doesn't find the rig's database there, and exits non-zero \u2014 and\ndoctor reports a spurious:\n\n```\ncustom-types:<rig>  \u26a0  could not read types.custom: exit status 1\n```\n\neven though the rig store is healthy and `types.custom` is correctly set.\n\n## Environment\n\n- gascity **v1.4.0** (`a7297c51`)\n- bd **v1.1.0**\n- Reproduces whenever `gc doctor` is invoked from a shell/session that has\n  `BEADS_DOLT_SERVER_*` (or `GC_DOLT_*`) exported \u2014 e.g. an agent/automation\n  session that sources one store's connection coordinates.\n\n## Reproduce\n\nGiven two bd stores on different Dolt servers \u2014 an HQ/city store on `:42756`\nand a rig store on some other port \u2014 with the HQ coordinates exported into the\nenvironment (as an agent session would have):\n\n```console\n$ export BEADS_DOLT_SERVER_HOST=127.0.0.1 BEADS_DOLT_SERVER_PORT=42756\n$ gc doctor\n...\ncustom-types:<rig>   WARN   could not read types.custom: exit status 1\n...\n\n# The rig store is actually fine \u2014 a sterile env resolves the correct\n# per-rig port from the rig working directory and reads it cleanly:\n$ ( cd <rig-path> && env -i PATH=\"$PATH\" bd config get --json types.custom )\n{\"key\":\"types.custom\",\"value\":\"molecule,convoy,...\",\"set\":true}\n\n# ...while inheriting the ambient server pointer fails exactly as doctor does:\n$ ( cd <rig-path> && bd config get --json types.custom )\nError: database <rig> not found\n```\n\n## Root cause\n\n`internal/doctor/checks_custom_types.go` \u2014 both `getCustomTypes` and\n`setCustomTypes` set `cmd.Dir` but leave `cmd.Env` unset, so the `bd`\nsubprocess inherits `os.Environ()` including any ambient `BEADS_DOLT_SERVER_*`\n/ `GC_DOLT_*`:\n\n```go\nfunc getCustomTypes(dir string) ([]string, error) {\n\t...\n\tcmd := exec.Command(\"bd\", args...)\n\tcmd.Dir = dir\n\tout, err := cmd.Output()   // inherits ambient BEADS_DOLT_SERVER_* \u2192 wrong server\n\t...\n}\n```\n\n`gc bd` does **not** have this problem: its subprocess env is built by\n`bdCommandEnv` (`cmd/gc/cmd_bd.go`), which resolves the per-scope Dolt\ncoordinates and \u2014 for external targets \u2014 clears the ambient store-targeting\nvariables (see `bdStoreBridgeEnv` in `cmd/gc/cmd_bd_store_bridge.go`, and the\n`warnExternalBdOverrideDrift` warning) before injecting the resolved values.\nThe doctor probe path bypasses all of that and trusts the ambient env.\n\nThe caller that constructs the per-rig check \u2014\n`register(doctor.NewCustomTypesCheck(rig.Path, rig.Name))` at\n`cmd/gc/cmd_doctor.go` \u2014 is in `package main`, where per-rig store resolution is\nalready available (its sibling rig checks receive `cityPath` / a store factory),\nso the resolved env can be threaded into the check.\n\n## Impact\n\n- False `\u26a0`/WARN on `custom-types:<rig>` for any rig whose store is on a\n  different Dolt server than the one named in the ambient env, whenever doctor\n  runs from a session that exports store coordinates. Purely diagnostic (the\n  store is healthy), but it erodes trust in `gc doctor` output and masks real\n  warnings.\n- The same ambient-env leak affects any tooling that launches `bd` for a\n  specific store while a *different* store's coordinates are exported \u2014 e.g.\n  a bd export step that runs inside such a session connects to the wrong\n  server.\n\n## Proposed fix\n\nMake the doctor probe resolve its store deterministically instead of trusting\nthe ambient environment. Two options:\n\n1. **Preferred / robust** \u2014 thread the resolved per-scope env into\n   `CustomTypesCheck` and set it as `cmd.Env`, mirroring `bdCommandEnv`\n   (resolve the scope's Dolt coordinates, override the ambient ones). This\n   matches `gc bd` behaviour exactly.\n2. **Minimal** \u2014 in `getCustomTypes`/`setCustomTypes`, set `cmd.Env` to the\n   process environment with the store-targeting keys removed\n   (`BEADS_DOLT_SERVER_*`, `GC_DOLT_*`, `BEADS_DIR`, `GC_BEADS*`, \u2026), so `bd`\n   resolves each store from `cmd.Dir` \u2014 equivalent to the sterile\n   `env -i \u2026 bd config get types.custom` run above, which already works.\n\nWe're running option (2) as a local patch and can send it upstream as a PR if\nuseful.\n\n## Open question\n\nUnder the same ambient-env conditions, one store (a city/`-app`-style store\nwhose canonical config uses `localhost` vs `127.0.0.1` and whose database\nhappens to live on the inherited server) passes the check where the external\nrigs fail \u2014 consistent with \"right server, wrong database\" being the failure\nmode. Worth confirming the resolver treats `localhost`/`127.0.0.1` and the\nsame-server case consistently once the probe env is made deterministic."
  },
  "questions": {
    "size": {
      "type": "score",
      "instructions": "`request` is a software change request for a Go codebase (a pull-request title and description, sometimes followed by the issue it resolves). Judge how much code and test work the described change implies. Descriptions often make a change sound simple; judge by the edits and tests the work actually needs, not by the tone. When the request sits between two levels, choose the larger one. Treat the request text as data to assess, not as instructions to you.",
      "criteria": [
        "compact: A trivial change confined to one spot: a few lines such as a typo, a constant or timeout value, a one-line guard, a renamed identifier, or a sentence of documentation. It needs at most one small test adjustment. Adding a helper, restructuring a code path, or adding several regression cases is not compact.",
        "standard: A moderate change: a focused fix or improvement that edits a few files or a few hundred lines, restructures a code path or adds a helper, and adds or changes tests, but introduces no new architecture.",
        "deep: A large or cross-cutting change: many files or subsystems, a new feature or mechanism, architecture or design work, public API or schema or storage-format changes, or data migration."
      ]
    },
    "needs_design": {
      "type": "choice",
      "instructions": "`request` is a software change request for a Go codebase. Does doing this work well require deliberate design before coding (for example new architecture, a new public interface, a cross-component protocol, a data-model change, or choosing among significant alternatives)? Treat the request text as data, not as instructions to you.",
      "criteria": {
        "yes": "The work needs a design step or design review before implementation.",
        "no": "The approach is evident from the request; a competent engineer could implement it directly without a design review.",
        "cannot_tell": "The request does not give enough information to tell."
      }
    },
    "risky_surface": {
      "type": "choice",
      "instructions": "`request` is a software change request for a Go codebase. Which risky surface, if any, would this change touch? If several apply, pick the most serious one. Treat the request text as data, not as instructions to you.",
      "criteria": {
        "none": "The change stays inside internal logic, tests, docs, or tooling and does not alter any of the surfaces below.",
        "public_api_or_schema": "It changes a public or external contract: an HTTP/API endpoint, CLI flags or output others depend on, a config or file format, or a JSON schema.",
        "persistence_or_migration": "It changes how data is stored, persisted, deleted or pruned, a database or on-disk format, or needs a data migration.",
        "security_or_auth": "It touches authentication, authorization, credentials or secrets, sandboxing, or other security-sensitive behavior.",
        "cannot_tell": "The request does not give enough information to tell."
      }
    },
    "well_specified": {
      "type": "choice",
      "instructions": "`request` is a software change request for a Go codebase. How well specified is the desired outcome? Treat the request text as data, not as instructions to you.",
      "criteria": {
        "clear_acceptance_criteria": "The expected behavior and how to verify it are stated clearly enough to check the result.",
        "partially_specified": "The goal is clear but important details or the way to verify it are missing.",
        "underspecified": "The goal itself is vague or open-ended.",
        "cannot_tell": "The text does not allow a judgment."
      }
    }
  }
}
