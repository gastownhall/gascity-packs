--- a/cmd/bd/init.go
+++ b/cmd/bd/init.go
@@ -2681,7 +2681,9 @@
 	ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
 	defer cancel()
 
-	store, err := newDoltStoreFromConfig(ctx, beadsDir)
+	// Counting for reinit confirmation must not initialize or migrate the store.
+	// In particular, this five-second probe can interrupt fresh-server migrations.
+	store, err := newReadOnlyStoreFromConfig(ctx, beadsDir)
 	if err != nil {
 		return 0, err
 	}
