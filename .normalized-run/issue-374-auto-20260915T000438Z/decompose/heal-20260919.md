# decompose heal — 2026-09-19

Operator heal per `decompose-hard-stop-diagnosis.md`:

1. Source binding decided and shipped: the runner now tracks a deterministic
   rig-store member bead instead of the graph.v2 `gcg-` step bead
   (workflows `9a75907a4`, `scripts/normalized_decompose_runner.py`).
2. Orphaned zero-member convoys `mc-djzpf` and `mc-ksm74` closed in the city store.
3. The zero-byte create fence `.input-convoy-create.json.tmp-87buvdsq` (durable proof
   of the interrupted create that produced `mc-djzpf`) consumed as part of this heal,
   together with the stale `.decompose-runner.lock`, so a `--resume` of this artifact
   dir can re-enter decompose instead of refusing on preserved evidence.
4. This run's roots were already purged; the decompose phase has no checkpoint to
   invalidate. A resume re-serves decompose fresh.
