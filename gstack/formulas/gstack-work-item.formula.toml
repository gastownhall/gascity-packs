description = """
gstack single-lane implementation item formula.
"""
formula = "gstack-work-item"
version = 1
contract = "graph.v2"
extends = ["do-work-item"]
target_required = true
internal = true
single_lane = true

[vars]
[vars.implementation_target]
description = "gstack task implementation role."
default = "gstack.implementer"

[[steps]]
id = "prepare-shared-worktree"
title = "Prepare gstack shared implementation worktree"
metadata = { "gc.run_target" = "gc.run-operator" }
description_file = "../assets/workflows/gstack-work-item/prepare-shared-worktree.md"

[steps.check]
max_attempts = 3

[steps.check.check]
mode = "exec"
path = "../assets/scripts/prepare-shared-worktree.sh"
timeout = "5m"

[[steps]]
id = "implement-item"
title = "Implement gstack shared-drain item"
needs = ["prepare-shared-worktree"]
metadata = { "gc.run_target" = "{{implementation_target}}", "gc.build.artifact_schema" = "gc.build.implementation-summary.v1", "gc.build.artifact_path_keys" = "gc.implementation.summary_path,gc.build.implementation_summary_path,gc.var.summary_path" }
description_file = "../assets/workflows/gstack-work-item/implement-item.md"

[steps.check]
max_attempts = 3

[steps.check.check]
mode = "exec"
path = ".gc/scripts/checks/build-artifact-valid.sh"
timeout = "5m"
