Write the failing test for this Superpowers implementation task.

Use the task context note, approved plan, and installed
`test-driven-development` guidance. First honor the task's approved file scope
and non-goals. If the approved task forbids test changes and an existing
focused test already fails for the required missing behavior, do not edit any
test file. Select that existing test as the failing evidence instead. Only add
or update the smallest test when approved scope permits test changes and no
existing test proves the behavior. Keep the test focused on the task bead's
acceptance criteria and avoid unrelated cleanup.

Record the exact test command that should fail and the expected failure reason.
Close this step only after the selected existing test or newly written test and
its command are ready to run from the task worktree.

Do not invoke provider-native subagents or upstream plugin runtime commands.
